import React, { Component } from "react";
import { FormApp } from "./FormApp";
// FormAppファイルのコンポーネントをimportしている
class App extends Component {
  render() {
    return (
      <FormApp />
      // FormAppファイルのコンポーネントを呼び出している
    );
  }
}

export default App;
